#include <bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin >> n;
    int maxlen = 0;
    vector<int> v(n);
    for(int i = 0; i < n; i++){
        cin >> v[i];
    unordered_map<int,int> um;
    um[0] = -1;
    int target;
    int sum = 0;
    cout << "What Sum : " << endl;
    cin >> target;
    for(int i = 0; i < n; i++){
        sum = sum + v[i];
        target = sum - f
        if(um.find(sum) != um.end()){

        }
    }
    }
}