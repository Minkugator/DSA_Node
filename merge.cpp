#include<bits/stdc++.h>
using namespace std;
int divide(vector<int> x,int m){
int siz = x.size();
if (m == siz){return 0;}
m = 0;
m++;
divide();
}
int main(){
    int n;
    cin >> n;
    vector<int> v(n);
    for(int i = 1; i <= n;  i++){
        cin >> v[i];
    }
    
}